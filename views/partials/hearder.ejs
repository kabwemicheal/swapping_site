<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <base href="/">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.2/css/all.css" integrity="sha384-oS3vJWv+0UjzBfQzYUhtDYW+Pj2yciDJxpsK1OYPAYjqT085Qq/1cq5FLXAZQ7Ay" crossorigin="anonymous">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css?family=Questrial&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
    <title>lets swap it</title>
    <style>
      
    </style>
</head>
<body>
    <nav id="bg_col" class="navbar navbar-expand-lg shadow-lg p-2 mb-4 fixed-top ">
        <div class="container-fluid">
           <a id="fsiz1" class="navbar-brand text-white" href="/">Letswap</a>
           <li id="fsiz2" class="nav mr-auto nav-item">
                <a class="nav-link text-white"  href="/items">items</a>
           </li>
        <ul id="fsiz2" class="nav justify-content-end ">
            <li class="nav-item">
                <a class="nav-link text-white"  href="/items/new"><i class="fa fa-plus" aria-hidden="true"></i></a>
            </li> 
            <% if(!currentUser){ %>
            <li class="nav-item">
                <a class="nav-link text-white " href="/login">login</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-white" href="/register">signup</a> 
            </li>
            <% } else { %>  
                <li class="nav-item">
                    <a class="nav-link text-white" href="/user/<%= currentUser._id %>" ><strong><i class="fa fa-user" aria-hidden="true"></i>&nbsp;<%= currentUser.username %></strong> </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white" href="/logout">Logout</a>
                </li>
           <% } %>  
        </ul>
        </div>
        </nav>
        <div id="mgt" class="container-fluid">
            <% if(error && error.length > 0){ %>
            <div class="alert alert-danger" role="alert">
                  <%=error%>
             </div>
             <% } %>
             <% if(success && success.length > 0){ %>
             <div class="alert alert-success" role="alert">
                  <%=success%>
             </div>
             <% } %>
        </div>
        
